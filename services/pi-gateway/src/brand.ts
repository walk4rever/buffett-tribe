/**
 * Brand identity for the agent gateway.
 *
 * Mirrors src/lib/brand.ts in the Next.js app — pi-gateway is a separate
 * deployable with its own package.json, so it cannot import from there.
 * Keep the two in sync when the brand changes.
 */
export const BRAND_ZH = "价值部落";
export const BRAND_EN = "Value Tribe";
