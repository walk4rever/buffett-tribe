{
  "name": "pi-gateway",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "sync:shared": "bash scripts/sync-shared-lib.sh",
    "dev": "npm run sync:shared && tsx watch --env-file=.env src/server.ts",
    "start": "npm run sync:shared && tsx --env-file=.env src/server.ts",
    "test": "vitest run",
    "reembed-wisdom": "tsx --env-file=.env scripts/reembed-wisdom.ts"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "^0.79.6",
    "cheerio": "^1.2.0",
    "express": "^4",
    "pg": "^8",
    "tsx": "^4",
    "typebox": "1.1.38"
  },
  "devDependencies": {
    "@types/express": "^4",
    "@types/node": "^22",
    "@types/pg": "^8",
    "typescript": "^5",
    "vitest": "^4.1.0"
  }
}
