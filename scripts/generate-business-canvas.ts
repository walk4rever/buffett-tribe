/**
 * Compatibility entrypoint.
 *
 * The business canvas generator now lives in generate-business-model.ts because
 * it generates both the business overview narrative and the 9-grid canvas.
 */
import "./generate-business-model";
